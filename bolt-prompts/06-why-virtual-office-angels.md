# Page 06 — Why Virtual Office Angels

Work only on `bolt/layout-enhance-1`. Read and edit only `src/pages/SourcePage.tsx`, `src/content/sourcePages.ts`, and why-page selectors in `src/styles/global.css`.

Polish only `WhyPage` at `/why-voa`. Present the existing managed-service benefits with premium editorial hierarchy, varied component scale, and restrained interaction. Use the full company name in visible copy. Avoid a repeated generic hero, giant cards, unsupported claims, and the same CTA treatment used on service pages. Do not modify other template functions.

Make one pass, run `npm run build` once, and stop on failure.
Before responding, perform the mandatory checkpoint from `claude.md`: commit this prompt's permitted changes and push only to `origin/bolt/layout-enhance-1`. Use `Bolt:` if complete and the check passed; use `WIP:` if incomplete or the check failed. Never push to `main`.
