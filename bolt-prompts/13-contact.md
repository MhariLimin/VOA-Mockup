# Page 13 — Contact

Work only on `bolt/layout-enhance-1`. Read and edit only `src/pages/SourcePage.tsx` and contact-page selectors in `src/styles/global.css`.

Polish only `ContactPage` at `/contact`. Preserve the existing contact details and prototype-form disclosure. Improve form grouping, labels, validation-ready states, keyboard focus, spacing, and responsive composition. Do not add a backend, email service, database, CAPTCHA, analytics, or new routes. Do not modify other `SourcePage` template functions.

Make one pass, run `npm run build` once, and stop on failure.
Before responding, perform the mandatory checkpoint from `claude.md`: commit this prompt's permitted changes and push only to `origin/bolt/layout-enhance-1`. Use `Bolt:` if complete and the check passed; use `WIP:` if incomplete or the check failed. Never push to `main`.
