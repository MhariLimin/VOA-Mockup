# Optional final verification — no redesign

Work only on `bolt/layout-enhance-1`. Do not redesign, refactor, add features, install packages, or inspect article bodies.

Run only `npm run lint` and `npm run build`. Preview the existing routes at representative mobile, tablet, and desktop widths and report concrete failures involving horizontal overflow, fixed-header clearance, keyboard navigation, theme contrast, missing images, or broken local links.

Do not fix anything during this prompt. Return a short numbered defect list with the responsible file for each issue, or report that no issue was found. Stop after the report.
