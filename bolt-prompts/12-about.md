# Page 12 — Our Story and Founder & Leadership

Work only on `bolt/layout-enhance-1`. Read and edit only `src/pages/SourcePage.tsx`, `src/content/sourcePages.ts`, and about-page selectors in `src/styles/global.css`.

Polish only `AboutPage` at `/about`, including `#story` and `#leadership`. Keep Our Story and Founder & Leadership visually and editorially distinct. Preserve the fuller source-grounded copy. Keep the founder portrait in the leadership section and do not reuse it as the generic page hero. Use the full company name in visible prose and do not invent biographical claims.

Make one pass, run `npm run build` once, and stop on failure.
Before responding, perform the mandatory checkpoint from `claude.md`: commit this prompt's permitted changes and push only to `origin/bolt/layout-enhance-1`. Use `Bolt:` if complete and the check passed; use `WIP:` if incomplete or the check failed. Never push to `main`.
