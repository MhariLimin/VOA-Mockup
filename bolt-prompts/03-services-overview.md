# Page 03 — Services overview

Work only on `bolt/layout-enhance-1`. Read and edit only `src/pages/SourcePage.tsx`, `src/content/sourcePages.ts`, and services-overview selectors in `src/styles/global.css`.

Polish only the `ServicesPage` rendering for `/services`. Keep all nine service destinations and existing source-grounded summaries. Make the directory compact, clearly grouped, easy to scan, and visually varied without oversized rows or cards. Do not change `ServicePage` or any other template function. Do not add content, routes, components, or dependencies.

Make one pass, run `npm run build` once, and stop. Report failures without automatically fixing unrelated files.
Before responding, perform the mandatory checkpoint from `claude.md`: commit this prompt's permitted changes and push only to `origin/bolt/layout-enhance-1`. Use `Bolt:` if complete and the check passed; use `WIP:` if incomplete or the check failed. Never push to `main`.
