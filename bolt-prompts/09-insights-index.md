# Page 09 — Articles and blog index

Work only on `bolt/layout-enhance-1`. Read and edit only `src/pages/SourcePage.tsx`, `src/content/blogContent.ts`, `src/content/source/staging/blogs-index.json`, and insights-index selectors in `src/styles/global.css`.

Polish only `InsightsPage` at `/insights`. Preserve all imported articles, filters, local routes, titles, dates, and corresponding local preview images. Keep cards compact and editorial. Never open all article-body JSON files and never link to the staging website. Do not modify `BlogArticlePage` in this task.

Make one pass, run `npm run build` once, and stop on failure.
Before responding, perform the mandatory checkpoint from `claude.md`: commit this prompt's permitted changes and push only to `origin/bolt/layout-enhance-1`. Use `Bolt:` if complete and the check passed; use `WIP:` if incomplete or the check failed. Never push to `main`.
