# Page 07 — Client stories and testimonials

Work only on `bolt/layout-enhance-1`. Read and edit only `src/pages/SourcePage.tsx`, `src/components/ui/ClientCarousel.tsx`, `src/content/source/staging/clients.json`, and story/carousel selectors in `src/styles/global.css`.

Polish only `StoriesPage` at `/client-stories` and its existing carousel. Retain all available client logos and testimonial text. Keep long copy left-aligned. The carousel must advance one client every two seconds, loop, pause during pointer/keyboard interaction, provide previous/next and Play/Pause controls, remain responsive, and respect reduced motion. This is the only looping carousel.

Do not inspect article data or modify other templates. Make one pass, run `npm run build` once, and stop on failure.
Before responding, perform the mandatory checkpoint from `claude.md`: commit this prompt's permitted changes and push only to `origin/bolt/layout-enhance-1`. Use `Bolt:` if complete and the check passed; use `WIP:` if incomplete or the check failed. Never push to `main`.
