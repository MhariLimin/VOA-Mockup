# Page 15 — Custom 404

Work only on `bolt/layout-enhance-1`. Read and edit only `src/pages/NotFoundPage.tsx` and 404-page selectors in `src/styles/global.css`.

Polish only the wildcard 404 into a branded, useful recovery page with concise navigation back to the homepage, Services, or Contact. Preserve the existing route behaviour. Do not add files, dependencies, tracking, or unrelated content.

Make one pass, run `npm run build` once, and stop on failure.
Before responding, perform the mandatory checkpoint from `claude.md`: commit this prompt's permitted changes and push only to `origin/bolt/layout-enhance-1`. Use `Bolt:` if complete and the check passed; use `WIP:` if incomplete or the check failed. Never push to `main`.
