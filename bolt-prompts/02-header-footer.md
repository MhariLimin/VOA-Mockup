# Page 02 — Global header and footer

Work only on `bolt/layout-enhance-1`. Modify only `src/components/layout/Header.tsx`, `Footer.tsx`, `PageShell.tsx`, `ThemeToggle.tsx`, `src/content/navigation.ts`, and directly related rules in `src/styles/global.css` or `themes.css`.

Create a fixed, precisely aligned header using the official transparent Virtual Office Angels logo. Keep comfortable navigation spacing and one primary CTA. Desktop dropdowns must open on click rather than hover alone and support keyboard operation, Escape, outside-click closing, visible focus, and ARIA state. Preserve every existing destination. Keep the mobile menu accessible and prevent the fixed header covering content. Polish the footer and theme toggle without changing page content.

Do not inspect or edit page components, articles, source data, or dependencies. Make one pass, run `npm run build` once, and stop. If it fails, report without an autonomous repair loop.
Before responding, perform the mandatory checkpoint from `claude.md`: commit this prompt's permitted changes and push only to `origin/bolt/layout-enhance-1`. Use `Bolt:` if complete and the check passed; use `WIP:` if incomplete or the check failed. Never push to `main`.
