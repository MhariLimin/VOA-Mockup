# Page 05 — How It Works

Work only on `bolt/layout-enhance-1`. Read and edit only `src/pages/SourcePage.tsx`, `src/content/homeContent.ts`, and process-page selectors in `src/styles/global.css`.

Polish only `ProcessPage` at `/how-it-works`. Preserve the existing process content. Create a compact, professional step sequence with strong hierarchy and restrained progress/connection motion. Avoid a generic hero, excessive vertical space, and duplicated CTAs. Do not modify any other page-template function.

Make one pass, run `npm run build` once, and stop. Do not diagnose unrelated failures.
Before responding, perform the mandatory checkpoint from `claude.md`: commit this prompt's permitted changes and push only to `origin/bolt/layout-enhance-1`. Use `Bolt:` if complete and the check passed; use `WIP:` if incomplete or the check failed. Never push to `main`.
