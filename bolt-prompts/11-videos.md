# Page 11 — Videos and resources

Work only on `bolt/layout-enhance-1`. Read and edit only `src/pages/SourcePage.tsx`, `src/content/sourcePages.ts`, and video-page selectors in `src/styles/global.css`.

Polish only `VideosPage` at `/videos`. Keep it clearly identified as a prototype awaiting approved videos. Improve composition and card proportions without fabricating titles, thumbnails, transcripts, URLs, or media. Do not create autoplay behaviour and do not modify other page templates.

Make one pass, run `npm run build` once, and stop on failure.
Before responding, perform the mandatory checkpoint from `claude.md`: commit this prompt's permitted changes and push only to `origin/bolt/layout-enhance-1`. Use `Bolt:` if complete and the check passed; use `WIP:` if incomplete or the check failed. Never push to `main`.
