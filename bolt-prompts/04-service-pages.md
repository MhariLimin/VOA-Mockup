# Page 04 — Shared service-detail family

Work only on `bolt/layout-enhance-1`. Read and edit only `src/pages/SourcePage.tsx`, `src/content/sourcePages.ts`, and service-detail selectors in `src/styles/global.css`.

Polish only the shared `ServicePage` rendering used by the nine `/services/...` routes. Preserve every route, task list, detail paragraph, source label, and assigned image. Keep imagery accurately mapped per service. Use a controlled editorial introduction rather than an oversized generic hero, compact task content, and a contextual page action instead of repeating the same generic CTA pair. Ensure the shared design works for all nine services without editing each route independently.

Do not alter other template functions, invent copy, add files, or add dependencies. Make one pass, run `npm run build` once, and stop on any failure.
Before responding, perform the mandatory checkpoint from `claude.md`: commit this prompt's permitted changes and push only to `origin/bolt/layout-enhance-1`. Use `Bolt:` if complete and the check passed; use `WIP:` if incomplete or the check failed. Never push to `main`.
