# Page 08 — Frequently asked questions

Work only on `bolt/layout-enhance-1`. Read and edit only `src/pages/SourcePage.tsx`, `src/content/faqContent.ts`, and FAQ selectors in `src/styles/global.css`.

Polish only `FaqPage` at `/faqs`. Preserve the source-derived questions and answers. Create an accessible, compact accordion with clear focus states, readable line lengths, and a restrained introduction rather than an oversized hero. Do not rewrite or shorten source answers and do not modify other page templates.

Make one pass, run `npm run build` once, and stop on failure.
Before responding, perform the mandatory checkpoint from `claude.md`: commit this prompt's permitted changes and push only to `origin/bolt/layout-enhance-1`. Use `Bolt:` if complete and the check passed; use `WIP:` if incomplete or the check failed. Never push to `main`.
