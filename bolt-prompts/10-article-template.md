# Page 10 — Shared article-detail template

Work only on `bolt/layout-enhance-1`. Read and edit only `src/pages/BlogArticlePage.tsx`, `src/content/blogContent.ts`, and article-detail selectors in `src/styles/global.css`.

Polish the shared `/insights/:slug` article template. Preserve article content, metadata, local images, and routing. Improve reading width, typography, media scale, navigation, and responsive behaviour. Do not open or rewrite the 30 article-body JSON files; the shared renderer must continue to load them unchanged. Do not add recommendations, related-article content, or claims that are not already present.

Make one pass, run `npm run build` once, and stop on failure.
Before responding, perform the mandatory checkpoint from `claude.md`: commit this prompt's permitted changes and push only to `origin/bolt/layout-enhance-1`. Use `Bolt:` if complete and the check passed; use `WIP:` if incomplete or the check failed. Never push to `main`.
